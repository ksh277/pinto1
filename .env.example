# --- App ---
NODE_ENV=development
APP_URL=http://localhost:3000

# --- MySQL ---
DATABASE_URL="mysql://USER:PASSWORD@HOST:3306/pinto?connection_limit=10&pool_timeout=30"

# 데이터 드라이버 스위치(임시 공존 가능): mysql | mock
DATA_DRIVER=mysql

# --- Auth (NextAuth) ---
AUTH_SECRET=랜덤_32바이트_시크릿
NEXTAUTH_URL=${APP_URL}

# --- Storage (S3 호환: AWS S3/Cloudflare R2/MinIO 중 택1) ---
S3_ENDPOINT=https://<your-endpoint>         # AWS면 비워도 됨
S3_REGION=ap-northeast-2
S3_BUCKET=pinto-uploads
S3_ACCESS_KEY_ID=xxx
S3_SECRET_ACCESS_KEY=xxx
S3_PUBLIC_BASE=https://cdn.example.com       # (선택) CloudFront/R2 Public URL

# --- Email (비번재설정/알림) 선택 ---
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=notify@example.com
SMTP_PASS=xxxx
SMTP_FROM="Pinto <notify@example.com>"
