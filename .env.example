# --- App ---
NODE_ENV=development
APP_URL=http://localhost:3000

# --- MySQL (Prisma) ---
# Example: mysql://user:password@127.0.0.1:3306/pinto
DATABASE_URL="mysql://USER:PASSWORD@HOST:3306/pinto?connection_limit=10&pool_timeout=30"

# Data driver (reserved): mysql | mock
DATA_DRIVER=mysql

# --- Auth (optional) ---
AUTH_SECRET=CHANGEME_32_BYTES_SECRET
NEXTAUTH_URL=${APP_URL}

# --- Storage (S3-compatible: AWS S3 / Cloudflare R2 / MinIO) ---
# If using AWS S3, S3_ENDPOINT can be omitted.
S3_ENDPOINT=
S3_REGION=ap-northeast-2
S3_BUCKET=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_PUBLIC_BASE=

# --- Email (optional) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Pinto <notify@example.com>"

